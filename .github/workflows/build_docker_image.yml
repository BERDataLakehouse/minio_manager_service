name: Build and push docker image

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:
    branches: [ "main", "master", "develop" ]
  release:
    types: [published]

jobs:
  build:
    uses: BERDataLakehouse/.github/workflow-templates/build_docker_image.yaml@main
    secrets: inherit